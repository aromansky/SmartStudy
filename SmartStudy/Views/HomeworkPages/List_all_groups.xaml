<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartStudy.Views.HomeworkPages.List_all_groups"
             Title="Ваши группы">
    <VerticalStackLayout>
        <ScrollView>
            <CollectionView x:Name="all_groups"
                        ItemsSource="{Binding GroupsWithUser}"
                        Margin="20"
                        SelectionMode="Single"
                        SelectionChanged="group_ckicked">

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Label Text="{Binding Title}" LineBreakMode="TailTruncation" FontSize="28" HorizontalOptions="Start"/>
                            <Label Text="{Binding Description}" LineBreakMode="TailTruncation" FontSize="20" TextColor="DarkGray"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>
    </VerticalStackLayout>
</ContentPage>

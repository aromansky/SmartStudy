<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartStudy.Views.CalendarPages.Group_list"
             Title="Группы">
    <ScrollView>
        <CollectionView x:Name="all_notes"
                    ItemsSource="{Binding Groups}"
                    Margin="20">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Title}" FontSize="28" HorizontalOptions="Start"/>
                        <HorizontalStackLayout>
                            <Label Text="{Binding Description}" MaxLines="1"  FontSize="20" TextColor="DarkGray" HorizontalTextAlignment="End"/>
                        </HorizontalStackLayout>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ScrollView>
</ContentPage>